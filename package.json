{
  "name": "fernanda-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "prepare:deploy": "node scripts/prepare-deploy.mjs",
    "deploy": "npm run prepare:deploy && wrangler pages deploy .dist --project-name fernanda-site --branch main"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
