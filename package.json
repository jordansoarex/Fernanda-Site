{
  "name": "fernanda-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name fernanda-site --branch main"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
